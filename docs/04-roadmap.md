# 未着手タスク・今後の検討事項

## 動作検証(最優先)

- [x] Wear OS実機またはエミュレータでのビルド・起動確認(Pixel Watch 4実機で確認済み)
- [x] `BODY_SENSORS` 権限フローの実機確認(実機での計測完了・スコア算出まで確認済み)
- [x] Health Servicesからの実際の心拍ストリーミング取得確認(実機計測でBPM取得・保存を確認済み)
- [x] Wearable Data Layerによる Wear→Mobile 実同期確認(実機で受信通知・ACKまで確認済み。接続断からの復帰遅延短縮は継続課題、[11-progress-2026-09-01.md](11-progress-2026-09-01.md) 参照)

## UI/UX

- [ ] アプリアイコン・スプラッシュ・テーマの整備(現状はOS標準テーマを流用)
- [x] Wear側: 計測中のフェーズ経過時間表示(実機インストール済み)
- [x] Wear側: 計測中の次段階・終了ボタンの誤タッチ対策(横並び配置・終了二段階確認、実機確認済み)
- [ ] Wear側: Ambient(常時表示)モード対応
- [x] Mobile側: セッション詳細画面(心拍推移グラフ、サイクルごとの内訳スコア`CycleScore`表示を実装・実機反映済み)
- [x] 履歴の個別・一括削除機能(実機で動作確認済み)
- [ ] 履歴の編集機能(未着手)

## アルゴリズム

- [ ] ととのい値の係数を実測データでチューニング
- [ ] 手首PPGのノイズ除去(体動アーティファクト対策)
- [ ] 心拍波形からのフェーズ自動検出(手動タグ付けの代替・補助)

## データ・同期

- [ ] クラウド同期: [12-cloud-migration-plan.md](12-cloud-migration-plan.md) に従い、Appwrite Cloud、Roomキャッシュ、WorkManager、tombstoneによるオフライン・ファースト同期を実装
- [ ] アカウント: メール/パスワード、Google OAuth、パスキーの実機スパイクと、アカウント連携・切替UIを実装
- [ ] Web管理画面: 閲覧、編集、削除、アカウント削除を備え、Cloudflare Workers Static Assetsへ独自サブドメインで配備
- [x] セッションのエクスポート(CSV形式の出力を実機確認済み)

## ブランディング・命名

- [ ] パッケージ名 `com.totonoi.sauna.*` の最終確認(必要であればより一般的な名称へ変更)
- [ ] アプリ名・アイコンが既存サービス(サの国/サウォッチ等)と混同されないか最終確認
